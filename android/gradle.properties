# CORREGIDO - Elimina duplicados y deja solo esto:
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
android.useAndroidX=true
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64
newArchEnabled=false
hermesEnabled=true

# SOLO estas líneas adicionales para WebRTC:
android.useFullClasspathForDexingTransform=true
android.enableJetifier=true
android.jetifier.ignorelist=bcprov

# ELIMINA estas líneas duplicadas:
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m (está duplicado)
# android.enableDexingArtifactTransform.desugaring=false (obsoleto)